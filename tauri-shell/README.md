# Paged.js Preview Tauri Shell

This folder is a thin desktop wrapper around the Flask app in the repository root.

See the root `README.md` for full install, `.venv`, development, and build instructions.
