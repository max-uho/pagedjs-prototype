[package]
name = "pagedjs-preview-shell"
version = "0.1.0"
description = "Desktop shell for the Paged.js Flask preview"
authors = ["Paged.js Prototype"]
license = ""
repository = ""
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
